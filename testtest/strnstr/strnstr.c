#include <string.h>

char	*strnstr(const char *big, const char *little, size_t len);

int main(void)
{
	const char *big1 = NULL;
	const char *little1 = NULL;
	size_t len1 = 0;
	strnstr(*big1, *little1, len1);

	const char *big;
	const char *little;
	size_t len;
	strnstr();
	
}
